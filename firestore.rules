rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Το backend χρησιμοποιεί service account που παρακάμπτει τα rules.
    // Το frontend δεν μιλάει απευθείας με Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
